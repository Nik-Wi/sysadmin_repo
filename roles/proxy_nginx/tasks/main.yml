- name: Install nginx
  ansible.builtin.apt:
    name: nginx
    state: present

- name: Copy proxy config template
  ansible.builtin.template:
    src: proxy.conf.j2
    dest: /etc/nginx/sites-available/default
  notify: Restart Nginx
